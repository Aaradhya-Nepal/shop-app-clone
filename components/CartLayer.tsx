export default function CartLayer({ open }: { open: boolean }) {
    return (
        <div className="cart-layer">
            <h2>Cart</h2>
            <p>This is the cart layer</p>
        </div>
    );
}
